version: 1
frontend:
  phases:
    preBuild:
      commands:
        - "curl -fsSL https://bun.com/install | bash"
        - "bun install"
    build:
      commands:
        - "bun run build"
  artifacts:
    baseDirectory: .output
    files:
      - "**/*"
